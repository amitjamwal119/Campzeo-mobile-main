export const unstable_settings = {
  headerShown: false, // this disables the auto header for this folder
};

export default function NotificationsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
